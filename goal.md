我在做一些多agent 的探索

我发现常常遇到的模式是，有一个大任务，其中有多个可以平凡并行的子任务
类似于：大任务 升级框架，子任务 升级模块1 升级模块2 升级模块3

还有些情况，任务之间有依赖关系，形成一个 DAG

之前我看到一个项目，可以把 tmux 里跑的agent显示在网页端
/home/jerrylu/code/260301-agent-viewer-fork

我在想，是否能实现一个系统，其中 用户可以输入任务（简单并行任务就是列表，复杂任务是 json 定义的 DAG，但无论如何都是静态的），可以指定输入prompt （静态的，或者一个动态 command 生成到stdout），然后系统会启动 agent 用 tmux 的方式输入 prompt，这样用户可以直接和进行中的 agent 交互（而不是 -p 那样只能看不能干预），一个 agent 完成之后继续触发满足了依赖关系的剩余任务 用户可以自己指定并行度 还有个简单的网页web ui 可以看到进展（只用列任务本身，tmux 用之前那个项目就可以看）

我想到的一个问题是，怎么判断任务完成？可能一个做法是让 agent 输出一个特殊的字符串，tmux 判断到了就认为完成了？（但是有可能和思维链冲突）；可能更好的是让 agent 运行一个 生成完成字符串的命令（例如 hmac 或者 sha 过），系统启动agent的时候就生成了，然后每隔几秒 capture tmux 输出比较是否存在？

默认给agent --yolo 权限

考虑用 python 来做，uv 来管理环境？

测试的时候用 codex

完成一个完整的项目 含readme.md 说明 中文
进展记录到 progress.md


